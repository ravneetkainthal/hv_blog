// types/types.ts

// Define the Blog type
export interface BlogType {
    _id: string;
    title: string;
    content: string;
    author: string;
    createdAt: string; // You can use a Date type if createdAt is a date
  }
  